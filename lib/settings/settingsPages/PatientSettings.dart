import 'package:flutter/material.dart';

class PatientsSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("PatientSettings"),
    );
  }
}
